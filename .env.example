# switchboard talks to any OpenAI-compatible gateway that fronts multiple
# providers (OpenAI, Anthropic, Google, ...) behind one key — e.g. a LiteLLM
# proxy. Set these to your own endpoint; do not commit real values.

OPENAI_API_KEY=
OPENAI_BASE_URL=
